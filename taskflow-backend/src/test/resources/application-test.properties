spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
  jpa:
   hibernate:
     ddl-auto: create-drop
  sql:
   init:
     mode: never

application:
  security:
    jwt:
      secret-key: test-secret-key
        expiration: 3600000